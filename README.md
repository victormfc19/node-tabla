# Notas

Programa en Node Js

```
Options:
      --help     Show help                                   [boolean]
      --version  Show version number                         [boolean]
  -b, --base     Es la base de la tabla de multiplicar        [number]
  -l, --list     Muestra la tabla en consola[boolean] [default: false]
  -h, --hasta    define el limite del multiplo                [number]

```

